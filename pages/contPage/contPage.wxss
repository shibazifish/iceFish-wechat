/* pages/contPage/contPage.wxss */
canvas{
  width: 370rpx;
  height: 370rpx;
  margin-left: calc((100% - 370rpx) / 2);
}
input{
  margin-left: calc((100% - 370rpx) / 2);
  margin-top: 50rpx;
}